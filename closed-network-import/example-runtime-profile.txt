# 예시 runtime profile 파일(Slice 99F) -- BATCH-CONVERT.cmd/.sh의 세 번째 인자로 넘긴다.
# 문법: 줄마다 정규 capability ID 하나. #으로 시작하는 줄은 전체가 주석. 빈 줄은 무시된다.
# ID는 반드시 com.example.xfdltracker.runtime.CommonRuntimeCapabilityCatalog.createSeeded()의
# 정규 카탈로그에 있는 값이어야 한다 -- 카탈로그에 없는 ID가 하나라도 있으면 변환 시작 전에
# 명시적으로 fail-closed된다(batch_runtime_profile_unknown_capability_id). 이 파일 자체는
# 예시일 뿐이며, 실제 값은 대상 폐쇄망 WebSquare 런타임에서 실제로 사용 가능한 capability만
# 나열해야 한다(사용 가능하다고 확인하지 않은 ID를 나열하지 말 것).
#
# 아무 capability도 사용 가능하지 않은 환경이라면, 이 파일을 비워두거나(모든 줄을 주석 처리)
# 파일 자체를 빈 상태로 두면 된다 -- 그 경우 TargetRuntimeProfile.empty()와 동등하게 처리된다.

MESSAGE_DIALOG
SCREEN_OPEN
